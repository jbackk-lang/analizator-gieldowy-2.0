fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
# WAZNE: swiezsza wersja yfinance (z curl_cffi) - Yahoo Finance ma ochrone
# antybotowa, stare wersje dostaja puste dane / JSONDecodeError zamiast
# prawdziwych notowan (ta sama lekcja co w analizator-gieldowy v1). run.bat
# wymusza "pip install --upgrade yfinance" wlasnie z tego powodu.
yfinance>=0.2.55
pytest>=7.0
httpx>=0.24
